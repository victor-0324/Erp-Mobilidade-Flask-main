# docker-compose.yml
version: '3.12'

services:
  web:
    build: .
    ports:
      - "5002:5002"
    environment:
      - FLASK_APP=wsgi.py
      - FLASK_ENV=production
      - DATABASE_CONNECTION=${DATABASE_CONNECTION}

    volumes:
      - .:/code

volumes:
  dbdata: